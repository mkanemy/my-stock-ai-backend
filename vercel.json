{
    "version": 2,
    "buildCommand": "npm run build",
    "functions": {
        "build/api/**/*.js": {
            "memory": 3000,
            "maxDuration": 10
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "build/api/index.js"
        }
    ]
}