{
    "version": 2,
    "buildCommand": "npm run build",
    "functions": {
        "build/api/**/*.js": {
            "memory": 3008,
            "maxDuration": 30
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "build/api/index.js"
        }
    ]
}